// 1. Function called divide that divides 2000 by 100
const divide = () => 2000 / 100;

// 2. Function called square that squares a number
const square = x => x * x;

// 3. Function called add that adds two numbers
const add = (a, b) => a + b;
